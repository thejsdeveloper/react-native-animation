import React from "react";
import { StyleSheet, Text, View } from "react-native";

const SwipeSurfing = () => {
  return (
    <View>
      <Text>Swipe Surfing</Text>
    </View>
  );
};

export default SwipeSurfing;

const styles = StyleSheet.create({});
