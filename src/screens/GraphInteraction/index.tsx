import React from "react";
import { StyleSheet, Text, View } from "react-native";

const GraphInteraction = () => {
  return (
    <View>
      <Text>GraphInteraction</Text>
    </View>
  );
};

export default GraphInteraction;

const styles = StyleSheet.create({});
